#[derive(Copy, Clone)]
pub enum Op {
    Push(i64),
    Add,
    Sub,
    Equal,
    LessThan,
    GreaterThan,
    LessThanEqual,
    GreaterThanEqual,
    BitwiseShiftRight,
    BitwiseShiftLeft,
    BitwiseOr,
    BitwiseAnd,
    If(usize),
    Else(usize),
    End(usize),
    Duplicate,
    Duplicate2,
    Dump,
    Drop,
    Do(usize),
    While,
    Mem,
    Load,
    Store,
    Syscall1,
    Syscall3,
}
